# Server Configuration (porta 3001 para não conflitar com outros apps na 3000)
PORT=3001
NODE_ENV=development

# Configuração por cliente (opcional; sem definir, usa defaults AltraFlow e localhost:3001)
# API_BASE_URL=http://localhost:3001
# ASSISTANT_NAME=AltraFlow
# COMPANY_NAME=AltraFlow
# SYSTEM_PROMPT_PATH=config/prompts/meu-cliente.md

# Mensageria / Produção (WhatsApp) — C14
# Na plataforma de mensageria, configure a URL do webhook: https://<seu-backend>/webhook/messages
# SOUCHAT_WEBHOOK_SECRET=your_webhook_secret_here   # Validar assinatura dos eventos
# SOUCHAT_API_KEY=your_souchat_api_key_here
# WEBHOOK_SECRET=alternativa_genérica_ao_SOUCHAT_WEBHOOK_SECRET

# AI Configuration (OpenRouter)
OPENROUTER_API_KEY=sua-chave-openrouter
OPENROUTER_MODEL=google/gemini-2.5-flash-lite

# Administração (projetos e agentes) — opcional
# Se definida, permite criar/editar empresas (tenants) e agentes via API e painel em /admin.
# Se vazia ou ausente, as rotas /api/admin/* retornam 401 (admin desabilitado).
# ADMIN_API_KEY=

# Persistência (Vercel Postgres)
# POSTGRES_URL=postgres://...

# Sessão/cache (Vercel KV / Redis)
# KV_REST_API_URL=https://...
# KV_REST_API_TOKEN=...

# Deploy CI não-interativo (opcional)
# VERCEL_TOKEN=
# VERCEL_SCOPE=
# VERCEL_PROJECT_NAME=qualy-altraia
